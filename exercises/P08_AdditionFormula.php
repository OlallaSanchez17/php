<?php

class P08_AdditionFormula {
    public function main(): void {
        // Define two numbers
        $numA = 2;
        $numB = 2;

        $sumas = $numA + $numB;

        // Output the formula and result
        echo "$numA + $numB = $sumas\n";
    }
}
